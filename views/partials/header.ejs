<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Series Tool v5</title>
    <link rel="shortcut icon" href="/icons/favicon.ico" type="image/x-icon">
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <!-- <link rel="stylesheet" href="css/main.css"> -->
    <link rel="stylesheet" href="<%= css %>">
    <link rel="stylesheet" href="<%= buttons %>">
    <script src="https://code.jquery.com/jquery-3.4.1.js" integrity="sha256-WpOohJOqMqqyKL9FccASB9O0KwACQJpFTUBLTYOVvVU="crossorigin="anonymous"></script>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
        <span class="navbar-brand">Series Tool v5</span>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item  <%= typeof page !== 'undefined' && page === 'home' ? 'active' : '' %>">
                    <a class="nav-link" href="/">Home</a>
                </li>
                <% if(page !== undefined && page === "showPage" || page === "editPage") { %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle <%= typeof page !== 'undefined' && page === 'showPage' ? 'active' : '' %>" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Serie Opties
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item <%= typeof page !== 'undefined' && page === 'editPage' ? 'active' : '' %>" href="/series/<%= foundSerie.title %>/edit">Edit</a>
                        <form class="delete-form" action="/series/<%= foundSerie.title %>?_method=DELETE" method="POST">
                            <button class="dropdown-item <%= typeof page !== 'undefined' && page === 'editPage' ? 'disabled' : '' %>">Verwijderen</button>
                        </form>                        
                    </div>
                </li>
                <% } %>
            </ul>

            <span class="nav-item <%= typeof page !== 'undefined' && page === 'registreren' ? 'active' : '' %>"><a class="nav-link" href="#">Registreren</a></span>
            <% if(error && error.length>0) { %>
                <div class="user messageError">
                    <%= error %>
                </div>
                <% } %>
                <% if(success && success.length>0) { %>
                <div class="user messageSuccess">
                    <%= success %>
                </div>
                <% } %>
        </div>
    </nav>
    <div class="container">
        